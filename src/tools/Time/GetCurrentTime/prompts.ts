export const DESCRIPTION = `这是一个获取当前时间的工具。

# 工具功能
此工具可以获取当前的本地时间信息，包括：
    - 当前时间戳
    - 格式化的时间字符串
    - 时间的各个组成部分（年、月、日、时、分、秒）
    - 本地时区信息
    - 星期几

# 返回格式
工具返回的时间信息包含以下字段：
    - **timestamp**: ISO 8601格式的时间戳
    - **formatted**: 人类可读的格式化时间字符串
    - **components**: 时间的各个组成部分
        - year: 年份
        - month: 月份（1-12）
        - day: 日期（1-31）
        - hour: 小时（0-23）
        - minute: 分钟（0-59）
        - second: 秒数（0-59）
    - **timezone**: 本地时区标识符
    - **weekday**: 星期几（如"Monday"、"Tuesday"等）



# 关键特点
1. **本地时区**: 工具自动使用机器的本地时区，无需用户指定
2. **实时性**: 每次调用都会获取最新的当前时间
3. **完整性**: 提供时间的所有相关信息，满足不同使用需求
4. **易读性**: 返回格式化的时间字符串，便于用户理解
5. **组合使用**: 可以与其他工具配合使用，可以用于以下场景：
    - 时间范围过滤：基于当前时间确定查询的时间范围（如本月、本周、上月等）

# 使用场景
**场景 1: 询问当前时间**
    **用户输入**: "现在几点了？"
    **操作**: 使用此工具获取当前时间并回答用户
**场景 2: 推算时间**
    **用户输入**: "这个月是几月？"
    **操作**: 使用此工具获取当前时间，然后推算出这个月是几月
**场景 3: 与其他工具配合**
    **用户输入**: "帮我看一下这个月我学了什么？"
    **操作**: 使用此工具获取当前时间，推算出这个月是几月，然后使用其他工具获取这个月的学习内容

# 注意事项
1. 时间信息基于用户机器的本地时区
2. 每次调用都会获取最新的时间，确保时间准确性
3. 工具不需要任何输入参数，直接调用即可获取当前时间
`;
