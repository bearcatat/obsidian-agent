// 您通常应该使用带有 'mv' 命令的 Bash 工具。对于较大的编辑，使用 Write 工具来覆盖文件。对于 Jupyter 笔记本（.ipynb 文件），请使用 ${NotebookEditTool.name}
export const DESCRIPTION = `这是一个用于编辑文件的工具。

使用此工具前：

使用 ReadNoteByPath/ReadNoteByLink 工具了解文件的内容和上下文


要进行文件编辑，请提供以下内容：
1. file_path：要修改文件的绝对路径（必须是绝对路径，不能是相对路径）
2. old_string：要替换的文本（在文件中必须是唯一的，并且必须与文件内容完全匹配，包括所有空格和缩进）
3. new_string：用于替换 old_string 的编辑后文本

该工具将在指定文件中用 new_string 替换 old_string 的一个实例。

使用此工具的关键要求：

1. 唯一性：old_string 必须唯一标识您要更改的特定实例。这意味着：
   - 包含更改点之前至少 3-5 行的上下文
   - 包含更改点之后至少 3-5 行的上下文
   - 包含文件中出现的所有空格、缩进和周围代码

2. 单实例：此工具一次只能更改一个实例。如果您需要更改多个实例：
   - 为每个实例单独调用此工具
   - 每次调用都必须使用广泛的上下文唯一标识其特定实例

3. 验证：使用此工具前：
   - 检查目标文本在文件中存在多少个实例
   - 如果存在多个实例，收集足够的上下文来唯一标识每个实例
   - 为每个实例计划单独的工具调用

警告：如果您不遵循这些要求：
   - 如果 old_string 匹配多个位置，工具将失败
   - 如果 old_string 不完全匹配（包括空格），工具将失败
   - 如果您没有包含足够的上下文，您可能会更改错误的实例

如果您想创建新文件，请使用：
   - 新文件路径，如果需要包括目录名
   - 空的 old_string
   - 新文件的内容作为 new_string

记住：当对同一文件进行多个文件编辑时，您应该更喜欢在单个消息中使用对此工具的多次调用来发送所有编辑，而不是在多个消息中每次使用单个调用。
记住：修改完后，不需要重复说明修改后的内容，用户有UI可以查看修改后的内容。`