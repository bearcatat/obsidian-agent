export const DESCRIPTION = `Gets the current local time.

Return format:
- timestamp: ISO 8601 format
- formatted: Human-readable time string
- components: year, month, day, hour, minute, second
- timezone: Local timezone identifier
- weekday: Day of the week (e.g., "Monday")

Features:
- Uses machine's local timezone
- No input parameters required
- Returns all time-related information

Usage scenarios:
- Answering time-related questions
- Calculating dates (e.g., "what month is it?")
- Filtering content by time ranges (this week, this month, etc.)
`;
